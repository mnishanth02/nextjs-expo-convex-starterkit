# Convex deployment (auto-generated by `pnpm dev:setup`)
CONVEX_DEPLOYMENT=

# IMPORTANT: The following env vars must be set in Convex deployment (not in .env.local)
# Set via: npx convex env set SITE_URL https://your-deployment.convex.site
#
# SITE_URL - Your Convex deployment URL (e.g., https://wooden-porcupine-536.convex.site)
#            This is used for OAuth redirect URIs

# Better Auth Secret (required for authentication)
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=

# Google OAuth (optional - for social sign in)
# Get credentials from: https://console.cloud.google.com/apis/dashboard
# These credentials are shared across web and native apps
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Apple OAuth (optional - for social sign in)
APPLE_CLIENT_ID=
APPLE_CLIENT_SECRET=
