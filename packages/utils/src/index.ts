/**
 * @repo/utils
 *
 * Shared utility functions for the monorepo.
 * Framework-agnostic, minimal dependencies.
 */

// Error utilities
export { getAuthErrorMessage, isAuthError, parseAuthError } from "./errors"

// ID utilities
export { generateUniqueId } from "./id"
